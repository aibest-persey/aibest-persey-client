# Web prod target: per aibest-persey-service's deploy model, the Express server
# serves the built client from the same origin as the API, so the base stays
# relative/empty. See ../aibest-persey-service/docs/SETUP.md ("Building for production").
VITE_API_URL=
